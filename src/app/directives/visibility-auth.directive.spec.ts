import { VisibilityAuthDirective } from './visibility-auth.directive';

describe('VisibilityAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityAuthDirective();
    expect(directive).toBeTruthy();
  });
});
